spring:
  datasource:
    username: postgres
    password: 12345
    url: jdbc:postgresql://192.168.48.131:5432/db_library

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

server:
  port: 8083
  servlet:
    context-path: /api
